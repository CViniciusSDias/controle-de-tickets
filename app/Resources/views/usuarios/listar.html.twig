{% extends 'layout.html.twig' %}

{% block titulo %}Usuários{% endblock %}

{% block conteudo %}
    <div class="row">
        <div class="box box-primary">
            <div class="box-body">
                <div id="tabela-usuarios">
                    <table class="data-table table table-bordered table-striped">
                        <thead>
                            <tr>
                                <th>Nome</th>
                                <th>E-mail</th>
                                <th>Tipo</th>
                                <th>Data de Cadastro</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for usuario in usuarios %}
                            <tr>
                                <td>{{ usuario.nome }}</td>
                                <td>{{ usuario.email }}</td>
                                <td>{{ usuario.nomeTipo }}</td>
                                <td>{{ usuario.dataCadastro|date('d/m/Y H:i') }}</td>
                                <td width="100">
                                    <form action="{{ path('remover_usuario') }}" method="post"
                                          onsubmit="return confirm('Deseja excluir {{ usuario.nome }}?');"
                                          style="display: inline-block;">
                                        <input type="hidden" name="id" value="{{ usuario.id }}">
                                        <button class="btn btn-danger">
                                            <i class="fa fa-trash"></i>
                                        </button>
                                    </form>

                                    <a href="{{ path('editar_usuario', {'id': usuario.id}) }}" class="btn btn-primary">
                                        <i class="fa fa-edit"></i>
                                    </a>
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock%}